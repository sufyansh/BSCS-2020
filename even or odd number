
#include <iostream>

using namespace std;

int main ()
{
    int a ,remainder;
    cout<<"enter number";
    cin>>a;
    remainder = a%2;
    if(remainder==0)
    cout<<" number is even";
    if (remainder!=0)
    cout<<"number is odd";
    return 0;
}
